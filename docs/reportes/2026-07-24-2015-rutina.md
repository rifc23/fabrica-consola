# Tick 20:15 UTC — 2026-07-24 (trigésimo séptimo disparo de `routine-fabrica-consola`)

## Anti-solape

`git fetch origin main`: último commit `04ab8a1` (~2h de antigüedad respecto a este disparo),
`git branch -r` solo devuelve `origin/main`, working tree limpio, sin worktrees huérfanos. Tick
procedió con normalidad.

## Inbox

`(vacío)` — sin triaje que hacer.

## Auditoría de estado real

- `routine-fabrica-consola` (`trig_01NduNpiSB2NsJNuCPxmpQQp`) verificada contra `list_triggers`:
  cron `15 */2 * * *`, `enabled: true`, `last_fired_at` coincide con este disparo. Sin
  discrepancias propias.
- **Hallazgo nuevo (no cosmético):** se amplió la auditoría más allá del trigger propio y se cruzó
  `list_triggers` contra la constante `CRON_TRABAJADORAS_POOL`/`CRON_DESPACHADORA_POOL` de
  `src/lib/cron.ts` (que alimenta el countdown de `EstadoPool` en el dashboard para CUALQUIER
  proyecto del Motor A-pool). Resultado:
  - `rutina-despachadora`: cron real `"5 * * * *"` — coincide con el código, sin acción.
  - `rutina-trabajadora-1`: cron real `"15 */2 * * *"` (cada 2h) vs. código `"10 * * * *"` (cada
    hora) — **desincronizado**.
  - `rutina-trabajadora-2`: cron real `"15 * * * *"` (cada hora) vs. código `"40 * * * *"` (cada
    hora, otro minuto) — **desincronizado**.
  - `updated_at` de ambos triggers: `2026-07-24T04:58–04:59 UTC` (~15h antes de este tick, cambio
    hecho fuera de este repo — no hay commit propio que lo explique).
  - Consecuencia verificada: el dashboard mostraba un countdown incorrecto para las trabajadoras
    del pool desde hace ~15h, para cualquier proyecto en el catálogo, no solo fabrica-consola.
  - Además, la reversión de `rutina-trabajadora-1` a cadencia de 2h contradice la decisión del
    usuario 2026-07-18 de bajar el ciclo del pool a 1h para ambas trabajadoras. No hay forma de
    saber desde este repo si fue intencional — se estacionó como pregunta nueva en
    `docs/backlog.md` § Decisiones estacionadas `[USUARIO]`.

## Trabajo del lote

Único ítem: corregir `CRON_TRABAJADORAS_POOL` en `src/lib/cron.ts` para reflejar la cadencia real
vigente (sin tocar ningún trigger real — eso es decisión de producto/riesgo fuera de este repo).
Delegado a un subagente `implementador` en worktree aislado (`model: sonnet`).

- **Diff:** `CRON_TRABAJADORAS_POOL` → `{1: "15 */2 * * *", 2: "15 * * * *"}`; nota de
  verificación agregada al JSDoc; `src/lib/cron.test.ts` actualizado (los dos `toBe(...)` del
  test `cronDeTrabajadoraPool`). Ningún otro archivo tocado.
- **Commits:** `f579703` (fix del subagente, rama `fix/cron-pool-sync-trabajadoras`) mergeado en
  `0e57922` sobre la rama del tick.
- **Gate corrido por mí en el checkout principal sobre el resultado del merge:** lint ✅,
  test:run **182/182** ✅ (mismo conteo — corrección de valores existentes, no tests nuevos),
  build ✅ (Next.js 16.2.11 / Turbopack, Node v22.22.2).
- Worktree del subagente limpiado (`git worktree remove` + rama temporal borrada) antes de cerrar
  el tick.

## Push

Rama designada `claude/rutina-2026-07-24-2015-cron-pool-sync` (toca `docs/backlog.md`,
`CLAUDE.md`, `docs/reportes/**` y además `src/lib/cron.ts`+`src/lib/cron.test.ts` — código, no
solo estado). Por el peldaño 4 (`.github/workflows/fabrica-sync.yml`), `fabrica-sync` debe
auto-mergearla a `main` tras correr el gate completo en CI; no toca `.github/**`, así que no
requiere merge humano.

## Decisiones estacionadas nuevas en este tick

- ¿El retorno de `rutina-trabajadora-1` a cadencia de 2h (`"15 */2 * * *"`) fue intencional, o
  quieres revertirlo a `"15 * * * *"` (cada hora, como `rutina-trabajadora-2`) para que el pool
  vuelva a la cadencia de 1h decidida el 2026-07-18? Ver `docs/backlog.md` § Decisiones
  estacionadas `[USUARIO]`.

## P1/P2

Sin ítems adicionales delegables — mismos bloqueos de siempre (Refinado instantáneo, Playwright
E2E, Motor B, `tipo:"gem"`, proxy de IA, reemplazo de `fire_trigger`), todos esperando decisión del
usuario. No es cierre de campaña: el hallazgo de este tick reabre trabajo (la pregunta estacionada
sobre `rutina-trabajadora-1`).
