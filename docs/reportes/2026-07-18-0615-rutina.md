# Reporte de rutina — 2026-07-18, disparo 06:15 UTC

Primer disparo real de `routine-fabrica-consola` (`trig_01NduNpiSB2NsJNuCPxmpQQp`, cron
`15 */2 * * *`).

## Anti-solape (Paso 0)

`git fetch origin main`: último commit `9c510d7` (~62 min antes del disparo), working tree limpio,
sin worktrees ni ramas huérfanas. Sin indicios de trabajo a medias → tick procedió.

## Triaje del Inbox

Sección `📥 Inbox` de `docs/backlog.md`: `(vacío)`. Sin entradas que procesar — no fue el trabajo
de este disparo.

## Verificación del entorno / gate

`npm install` + gate completo corrido de verdad ANTES de tomar cualquier tarea: `npm run lint`
✅, `npm run test:run` → **107/107** ✅, `npm run build` ✅ (Node v22.22.2, Next.js 16.2.10 /
Turbopack). El entorno puede correr el gate sin pasos adicionales.

## Auditoría del estado real (antes de tomar tareas)

- La fila del Registro de trabajo del backlog para "estado del deploy en preview + aviso claro de
  routine pendiente" decía **"Pendiente de merge por el usuario"**, pero `git log` confirma que ya
  está en `main` desde el 2026-07-17 (`6520bd4`). Corregida con el hash real.
- `CLAUDE.md` § Ancla de rollback apuntaba al commit inicial de Fase 0-1 (2026-07-14), muy
  desactualizado frente al HEAD real de `main` (`9c510d7`, con las 5 P0 del MVP + fixes ya
  mergeados). Actualizado.
- No existe `.fabrica.json` en la raíz de este repo — correcto: fabrica-consola no se autogestiona
  como proyecto hijo (ese manifest es para los repos que ELLA crea). El paso genérico de la
  plantilla "actualizar `ultimo_tick` en `.fabrica.json`" no aplica aquí y se omitió sin sustituto.

## Lote del disparo: P1 completo (Gem, cola/tiempos, burndown)

Única cola no-P0 pendiente del backlog en este momento — las tres tareas sin archivos fuente
compartidos entre sí, delegadas en paralelo a subagentes `implementador` (modelo `sonnet` explícito,
worktree aislado cada uno, prompt autocontenido):

| Tarea | Rama del subagente | Commit | Gate del subagente |
|---|---|---|---|
| Tipo de proyecto "Gem" en el formulario | `feat/gem-tipo-proyecto` | `e2db52e` | lint ✅ test:run 117/117 ✅ build ✅ |
| Vista de cola y tiempos en el dashboard | `feat/cola-y-tiempos-dashboard` | `0e3647e` | lint ✅ test:run 118/118 ✅ build ✅ |
| Burn-down del backlog | `feat/burndown-backlog` | `1fd672f` | lint ✅ test:run 122/122 ✅ build ✅ |

Integración en `claude/rutina-2026-07-18-0615-p1-batch`:
1. Merge de `feat/gem-tipo-proyecto` → `8158d1d` (sin conflictos).
2. Merge de `feat/cola-y-tiempos-dashboard` → `69ba763` (sin conflictos).
3. Merge de `feat/burndown-backlog` → **conflicto esperado** en
   `src/app/proyectos/[id]/page.tsx` (ambas tareas añaden su propia sección al mismo dashboard).
   Resolución delegada a un CUARTO subagente `implementador` (no resuelta a mano por el
   orquestador — regla de separación de modelos aplica también a la resolución de conflictos de
   merge): combina ambas secciones de forma aditiva. Commit de integración: `f80f0f5`.

**Gate final, corrido por el orquestador en el checkout principal sobre el resultado integrado:**
`npm run lint` ✅, `npm run test:run` → **143/143** ✅, `npm run build` ✅.

Diff final vs `main`: 21 archivos, +1237/-22 — alcance exactamente el esperado (Gem +
cola/tiempos + burndown + integración del dashboard + consolidación de docs), sin cambios fuera de
alcance.

## Hallazgos de infraestructura (documentados, no bloquean el lote)

- **Falso positivo de lint por worktrees de subagentes**: al correr el gate final con los 4
  worktrees de subagentes aún presentes (`.claude/worktrees/agent-*`, cada uno con su propio
  `.next/` de build), `npm run lint` reportó 640+ errores — el `globalIgnores(".next/**")` de
  `eslint.config.*` está anclado a la raíz del repo y no cubre `.next/` anidados dentro de
  worktrees. Diagnosticado, worktrees ya integrados eliminados con `git worktree remove` (sin
  cambios pendientes, verificado antes de borrar), gate re-corrido limpio. Documentado como Error
  Conocido en `CLAUDE.md` (regla: limpiar worktrees integrados antes del gate final) y como P2 en
  el backlog (corrección de raíz: ampliar el patrón de ignore).
- **`src/lib/cron.ts` ya existía** (del P0 de decisiones respondibles) — el subagente de
  cola/tiempos lo detectó y extendió en vez de duplicarlo.
- **`.fabrica.json`.`tipo`** se tipó localmente en `route.ts` en vez de en `FabricaManifest`
  (`src/lib/github.ts`) para no arriesgar colisión con las tareas hermanas — anotado en el backlog
  como P2 si se agregan más tipos de proyecto.

## Estacionado

Ninguna decisión nueva estacionada este tick. Las dos ya existentes (diseño visual, nombre del
producto) siguen esperando al usuario — sin cambios.

## Estado final

- Rama `claude/rutina-2026-07-18-0615-p1-batch` pusheada (toca código: `src/**` — el usuario debe
  mergearla a mano, `fabrica-sync.yml` NO la auto-mergea).
- Backlog: P1 queda con las tres tareas en `[x]`. Cola no-P0 restante: **vacía** — no queda ningún
  ítem P1/P2 delegable sin decisión de usuario en este momento (P2 son mejoras opcionales/deuda,
  ninguna bloqueante). Este NO es el cierre de campaña porque el usuario aún debe mergear código
  pendiente (esta rama) antes de que el backlog quede realmente al día en `main`; el siguiente
  disparo reevaluará con `main` ya actualizado (o no, si el merge sigue pendiente).
