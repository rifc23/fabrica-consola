# Tick 18:15 UTC — `routine-fabrica-consola` (séptimo disparo)

## Anti-solape

`git fetch origin main`: último commit `3155ca3` ("merge: botón Asignar ahora + ciclo del pool
reducido a 1h"), ~28 minutos de antigüedad al iniciar el tick (commit 17:47:44 UTC, tick 18:15:38
UTC) — por encima del umbral de 12 minutos. Working tree limpio, sin worktrees ni ramas huérfanas
(`git worktree list` solo muestra el checkout principal; `git branch -r` solo `origin/main`). Tick
procede con normalidad.

## Memoria leída

`docs/backlog.md` completo, `docs/TAREAS-MANUALES.md` completo, `CLAUDE.md` completo (REGLAS NO
NEGOCIABLES, Regla de despliegue seguro, Decisiones Arquitectónicas, Errores Conocidos, Ancla de
rollback).

## Triaje del Inbox

**Hallazgo:** la sección `📥 Inbox` de `docs/backlog.md` **no existía** en el archivo (solo había
encabezados `## ` para Estado general, P0, P1, P2, Decisiones estacionadas y Registro de trabajo).
Sin embargo, el protocolo de cabecera del propio backlog, `CLAUDE.md` y los seis reportes de tick
anteriores (06:15 a 16:15 UTC del mismo día) afirmaban "Inbox: (vacío)" como si la sección
existiera y estuviera vacía. Una entrada de la sección Estado general (2026-07-18, hallazgo del
trigger fantasma) llegó a afirmar explícitamente "Aprovechado el mismo hallazgo para agregar la
sección `📥 Inbox` que faltaba en este backlog" — afirmación falsa, nunca se agregó.

Investigado con un subagente de exploración de solo-lectura antes de tocar nada:
- `src/lib/backlog.ts::insertarEnInbox` exige un encabezado `/^##\s.*Inbox/` y lanza
  `Error("El backlog no tiene sección '📥 Inbox' — no se puede insertar la entrada.")` si falta.
- No era explotable hoy: consultada la API de GitHub, `rifc23/fabrica-consola` no lleva ningún
  topic (`{"names": []}`), así que `obtenerProyectos`/`FABRICA_TOPIC` nunca lista a fabrica-consola
  en su propio dropdown y `POST /api/tareas` bloquea igual cualquier escritura a un repo fuera de
  esa lista — nadie pudo haber disparado el error en producción.
- Causa raíz: `sembrarBacklogNuevoProyecto` (que sí agrega la sección) solo corre al crear
  proyectos NUEVOS desde el formulario; fabrica-consola se bootstrapeó a mano el 2026-07-14, antes
  de que existiera ese patrón (decidido 2026-07-17), y nunca se retrofit.

**Corrección aplicada** (única escritora: este tick, como orquestador):
1. Agregada la sección real `## 📥 Inbox — entradas del usuario (sin triaje)` entre "Estado
   general" y "P0", con el mismo formato que usa el fixture de test
   (`src/lib/backlog.test.ts`) — vacía (`- (vacío)`), sin entradas que triajar.
2. Corregida la afirmación falsa de 2026-07-18 en Estado general con una nota fechada explicando
   qué pasó y por qué no fue explotable.
3. Nuevo Error Conocido en `CLAUDE.md`: es la segunda vez que un registro de "ya está hecho" en el
   backlog resulta falso sin que nadie lo note por varios ticks (la primera fue el trigger
   `trig_01XJA8ejJVsh1aQE4fZFdeN1` que nunca se creó). Regla agregada: ninguna sesión marca algo
   como agregado/corregido sin verificar el resultado en el diff real.

Sin entradas reales que procesar (la sección recién agregada nace vacía) — no hay triaje de tareas
de usuario este tick.

## Auditoría de estado real

`main` había avanzado **20 commits** desde la última Ancla de rollback verificada en `CLAUDE.md`
(tick 16:15 UTC, `15cebdb`) sin que esa sección lo reflejara. El trabajo: **Motor A-pool completo**
— helpers de lock optimista (`trabajadorasLibres` en `src/lib/github.ts`), estado del pool en la
cola del dashboard (`EstadoPool`), decisión "el pool es el motor DEFAULT" (retira el flujo de
instalar routine dedicada de `/nuevo-proyecto`), routine madre actualizada a v4 (PASO 4: despacho
de emergencia — asigna lock y dispara con `fire_trigger` si un proyecto sin `trigger_id` no tiene
trabajadora asignada), botón "🔧 Asignar ahora" (`/api/asignar-proyecto`), y ciclo del pool
reducido de 2h a 1h. Se implementó y mergeó directo a `main` en una **sesión interactiva del
usuario** (commits con timestamp local `-0400`, entre el cierre del tick de las 16:15 UTC y este
disparo) — no dejó reporte en `docs/reportes/` porque no lo ejecutó la routine.

La narrativa de Estado general de `docs/backlog.md` ya documentaba este trabajo (la propia sesión
interactiva la fue actualizando), pero:
- La tabla **Registro de trabajo** no tenía ninguna fila para estos 20 commits — agregada una fila
  resumen con los hashes principales.
- La **Ancla de rollback** de `CLAUDE.md` seguía apuntando a `15cebdb` — corregida al HEAD real
  (`3155ca3`).
- **Decisiones Arquitectónicas** de `CLAUDE.md` no mencionaba el Motor A-pool en absoluto pese a
  ser una decisión arquitectónica mayor ya implementada — agregado un bullet completo.

`git merge-base --is-ancestor` confirma que no queda ninguna rama `claude/*` huérfana (`git
branch -r` solo lista `origin/main`); todo el trabajo del pool y de ticks anteriores ya está
integrado.

## Gate (real, corrido en este entorno)

```
npm install   → 448 paquetes, sin errores bloqueantes (2 vulnerabilidades moderadas preexistentes,
                no relacionadas con este tick)
npm run lint  → ✅ sin errores
npm run test:run → ✅ 161/161 (subieron de 143 por las pruebas del Motor A-pool)
npm run build → ✅ Next.js 16.2.10 / Turbopack, Node v22.22.2
```

## P1 / P2 — auditoría de lo delegable

- **P1:** las tres tareas (Gem, cola/tiempos, burn-down) ya están `[x]` y en `main`. Sin ítems
  nuevos.
- **P2:** revisado ítem por ítem, mismos bloqueos que ticks anteriores — ninguno delegable sin
  decisión del usuario:
  - Refinado instantáneo del feedback: requiere reponer la tarea manual de `ANTHROPIC_API_KEY`.
  - Playwright E2E: requiere autorización para crear un repo/proyecto Vercel de prueba reales (o
    decisión de usar mocks) — sigue estacionado en Decisiones [USUARIO].
  - Motor B: explícitamente fuera de v1.
  - Promover `tipo?: "gem"` a campo real: esperando un segundo tipo de proyecto.
  - Fix de `globalIgnores`/`vitest` `.claude/**`: ya resuelto en el tick 16:15 UTC.

**Sin trabajo nuevo delegable este disparo** más allá de la corrección de documentación de arriba.

## Cambios de este tick

Solo documentación (`docs/backlog.md`, `CLAUDE.md`, este reporte) — ningún subagente `implementador`
fue necesario porque el trabajo era exclusivamente auditoría y corrección de las dos fuentes de
verdad que solo el orquestador edita. Rama `claude/rutina-2026-07-18-1815-auditoria`, diff
solo-estado → `fabrica-sync.yml` la publica a `main` automáticamente, sin acción del usuario.

## Decisiones estacionadas (sin cambios este tick)

Las mismas tres de siempre — diseño visual, nombre del producto, y Playwright E2E — ninguna
respondida en el Inbox (que sigue, correctamente, vacío tras agregarse la sección real).
