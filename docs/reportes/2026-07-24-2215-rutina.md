# Reporte — tick 22:15 UTC, 2026-07-24 (trigésimo octavo disparo de `routine-fabrica-consola`)

## Anti-solape

`git fetch origin main`: último commit `8bc1fda` ("fabrica-sync: código desde
`claude/rutina-2026-07-24-2015-cron-pool-sync`", peldaño 4), ~1h50min de antigüedad en el momento
del fetch — no coincide con el patrón de "commit reciente propio" (>12 min). Working tree limpio,
sin worktrees ni ramas huérfanas (`git branch -a` solo devuelve `main`). Tick procedió con
normalidad.

## Memoria leída

`docs/backlog.md` completo, `docs/TAREAS-MANUALES.md` completo, `CLAUDE.md` completo (REGLAS NO
NEGOCIABLES, Regla de despliegue seguro, Errores Conocidos, Ancla de rollback).

## Triaje del Inbox

Sección `📥 Inbox` de `docs/backlog.md`: `(vacío)`. Sin triaje que hacer.

## Auditoría de estado real

- `git log 04ab8a1..8bc1fda` confirmó que **solo** el trabajo esperado del tick 20:15 UTC
  (`b4f528a`, `f579703`, `0e57922`, más el commit de sync `3559470` y el merge final `8bc1fda`)
  llegó a `main` desde la última ancla verificada — sin ramas huérfanas ni trabajo inesperado.
- La fila del tick 20:15 UTC en el `Registro de trabajo` decía "pendiente de fabrica-sync" pese a
  que ya estaba mergeado en `8bc1fda` — corregida.
- `src/lib/cron.ts` verificado contra el hallazgo del tick anterior: `CRON_TRABAJADORAS_POOL` sigue
  con `{1: "15 */2 * * *", 2: "15 * * * *"}`, coincide con lo documentado.
- `TAREAS-MANUALES.md`: sin tareas nuevas, solo la 🟡 3 (diseño visual/nombre) sigue pendiente sin
  bloquear.
- P0/P1: sin cambios, todo `[x]`.

## Entorno y gate

`npm ci` + gate completo real sobre `main` (antes de cualquier intento de cambio):
- `npm run lint` → ✅ sin errores.
- `npm run test:run` → ✅ **182/182** (sin cambio).
- `npm run build` → ✅ (Next.js 16.2.11/Turbopack, Node v22.22.2).

## Hallazgo nuevo: `npm audit` subió de 3 a 12 vulnerabilidades altas

`npm audit --audit-level=high` reportó 12 vulnerabilidades altas, no las 3 documentadas
(`postcss`/`sharp`, pineadas por `next@16.2.11`, sin acción posible — sin cambio). Las 9 nuevas son
de `brace-expansion@1.1.16` (GHSA-mh99-v99m-4gvg, DoS por expansión de longitud no acotada),
arrastradas por toda la cadena de tooling de lint: `eslint`→`@eslint/config-array`/
`@eslint/eslintrc`→`minimatch@3.1.5`, más `eslint-config-next` y sus plugins
(`eslint-plugin-import`/`jsx-a11y`/`react`). Es dependencia de **desarrollo** únicamente — no llega
al bundle ni al runtime desplegado en producción.

### Intento de fix (delegado, investigado, revertido)

Delegué a un subagente `implementador` en worktree aislado la tarea de aplicar el patch real
(`brace-expansion@5.0.8`, confirmado vía `npm view` como el único release con el fix). El subagente
hizo **repro-primero** antes de comprometerse y encontró que la premisa de la tarea era falsa para
una parte del árbol de dependencias:

- `brace-expansion` publica **5 líneas mayores paralelas** (`1.x`–`5.x`) que no interoperan; el fix
  del CVE solo se backporteó a la línea `5.x`.
- El árbol de este repo tiene DOS instancias: una vía `@typescript-eslint/typescript-estree`→
  `minimatch@10.x` (ya en la línea `5.x`, bump seguro), y otra vía `eslint@9.39.5` (única versión
  `9.x` disponible)→`@eslint/config-array`→`minimatch@3.1.5`→`brace-expansion@1.1.16`, que usa la
  API vieja (`require('brace-expansion')` como función, no exportación nombrada).
- Forzar `brace-expansion@5.0.8` en la segunda instancia rompe `npm run lint` de verdad:
  `TypeError: expand is not a function` — probado, no hipotético.
- Un override acotado solo a la primera instancia deja el gate verde pero `npm audit` sigue en 12
  (el advisory se dispara por paquete, no por instancia individual).
- El único camino real para eliminar las 9 es un bump **mayor** de `eslint` a `10.8.0` — que la
  tarea delegada prohibió explícitamente por el riesgo de breaking change en reglas de lint
  activas.

El subagente revirtió todo (`git checkout -- package.json package-lock.json`, `npm ci`), sin
ningún commit, working tree limpio verificado, en vez de dejar un fix parcial o roto en la rama —
correcto según la regla de repro-primero y "código completo o revertir a estado consistente" de
`docs/backlog.md`.

### Acción tomada

- Nuevo ítem `[ ]` en P2 de `docs/backlog.md` documentando el hallazgo completo.
- Nueva pregunta en "Decisiones estacionadas [USUARIO]": ¿autorizar el bump mayor de `eslint` a
  `10.8.0` (tarea dedicada, con su propio gate y revisión de reglas de lint), o aceptar el riesgo
  residual (dev-only, no explotable en este contexto) hasta que `eslint` publique un patch en la
  línea `9.x` o `brace-expansion` backportee el fix a la línea `1.x`?

## Registro de trabajo

Sin código nuevo en producción este tick — el único intento de cambio se revirtió sin commit. Fila
agregada al `Registro de trabajo` de `docs/backlog.md` documentando el hallazgo y la corrección de
la fila anterior. Rama de este tick: `claude/rutina-2026-07-24-2215-audit-deps-security` (solo
`docs/backlog.md` + `CLAUDE.md` + este reporte) — auto-mergeable por `fabrica-sync` (solo-estado).

## P1/P2 revisados

Sin ítems nuevos delegables además del hallazgo de arriba — mismos bloqueos por decisión de usuario
que ticks anteriores: Refinado instantáneo y Playwright E2E estacionados, Motor B no es v1,
`tipo:"gem"` condicionado a un segundo tipo de proyecto, `postcss`/`sharp` pineados por Next sin
acción posible, mecanismo de reemplazo de `fire_trigger` sin decisión del usuario, cadencia real de
`rutina-trabajadora-1` sin decisión del usuario, y ahora el bump mayor de `eslint` sin decisión del
usuario.

## Estado de campaña

No es cierre de campaña: hay una pregunta nueva estacionada esperando decisión del usuario
(bump de `eslint`), y las estacionadas previas siguen abiertas. Vigésimo cuarto tick consecutivo
(desde 2026-07-18 12:15 UTC) sin trabajo P1/P2 estructural nuevo delegable, pero el segundo tick
consecutivo (tras el bump de seguridad de Next.js del 16:15 UTC) en encontrar y auditar una
vulnerabilidad real vía `npm audit` — la rutina de auditoría de seguridad periódica está
funcionando como se espera, aunque este hallazgo específico no tenga fix seguro disponible hoy.
