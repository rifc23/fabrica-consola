# Tick 06:15 UTC (2026-07-19) — `routine-fabrica-consola`

## Anti-solape

`git fetch origin main`: el local estaba **67 commits detrás** de `origin/main` (última
sincronización local databa del tick 16:15 UTC del 2026-07-18). Fast-forward limpio a `df8c9db`.
El commit más reciente (`df8c9db`, ~1h49min de antigüedad al momento del fetch) no coincide con el
patrón propio de esta routine (es un merge directo de sesión interactiva del usuario) y el working
tree/worktrees estaban limpios, sin ramas huérfanas (`git branch -r` solo `origin/main`) →
**tick procedió con normalidad**.

## Inbox

`(vacío)` → sin triaje.

## Auditoría de estado real

Encontrados **2 merges directos a `main`** (sesión interactiva del usuario, entre la ancla anterior
`a0366c1` y el HEAD actual `df8c9db`) sin fila en `docs/backlog.md` § Registro de trabajo ni
mención en `CLAUDE.md` § Ancla de rollback — mismo patrón de "hecho sin registrar" ya documentado en
`CLAUDE.md` § Errores Conocidos:

1. **`2ffe055`/`40717a4`** — fix: GitHub rechazaba con 422 ("Description control characters are not
   allowed") la creación de repos cuyo objetivo (textarea multilínea) traía saltos de línea/tabs sin
   sanear. Nueva función pura `descripcionRepoDesdeObjetivo` (`src/lib/formulario-proyecto.ts`, con
   tests).
2. **`01325e4`/`df8c9db`** — fix: el formulario "Nuevo proyecto" ocultaba el `<form>` por completo
   tras un error de creación sin forma de volver a verlo (recargar la página borraba lo escrito,
   estado solo en memoria). Botón "Volver al formulario" en `ProgresoCreacion` + persistencia del
   borrador en `localStorage` (`src/lib/borrador-nuevo-proyecto.ts`, 9 tests).

Ambos hallazgos corregidos con filas nuevas en el Registro de trabajo y la Ancla de rollback
actualizada al HEAD real (`df8c9db`). También corregida la fila del tick 04:15 UTC, que decía
"pendiente de push" pese a que `fabrica-sync` ya la había integrado (`11c250c`).

## Gate real (entorno re-verificado con `npm ci`)

- lint ✅
- test:run **182/182** ✅ (subieron de 168 — 35 tests nuevos entre ambos fixes)
- build ✅ (Next.js 16.2.10/Turbopack)

## P0/P1/P2

- P0/P1: sin cambios, todo `[x]`.
- P2 revisado ítem por ítem: **sin ítems nuevos delegables** — mismos bloqueos por decisión de
  usuario que ticks anteriores (Refinado instantáneo y Playwright E2E estacionados en "Decisiones
  estacionadas [USUARIO]", Motor B no es v1, promover `tipo:"gem"` sigue condicionado a un segundo
  tipo de proyecto, proxy de IA Paquetes 1/2 fuera del alcance autónomo de esta routine por
  gobernanza explícita del usuario).

## Resultado de este tick

Solo documentación (corrección de registro histórico + gate re-verificado). Sin código nuevo. Rama
`claude/rutina-2026-07-19-0615-auditoria`, diff limitado a `docs/backlog.md` y `CLAUDE.md` →
auto-mergeable por `fabrica-sync.yml` sin acción del usuario.
