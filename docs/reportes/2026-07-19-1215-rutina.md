# Reporte — routine-fabrica-consola, tick 2026-07-19 12:15 UTC (decimosexto disparo)

## Anti-solape (PASO 0)

`git fetch origin main`: último commit `39bf9e4` ("docs: documentar fallo estructural de
`fire_trigger` en routine madre (PASO 1)"), autor `rifc23` (sesión interactiva/routine madre, no un
patrón de merge de `fabrica-sync` ni de esta routine), ~1h21min de antigüedad al momento del fetch.
Working tree limpio, sin worktrees ni ramas huérfanas (`git branch -r` solo devuelve `origin/main`).
Sin indicio de trabajo a medias → tick procedió con normalidad.

## Memoria

Leídos `docs/backlog.md` completo, `docs/TAREAS-MANUALES.md` y `CLAUDE.md`. Sin `.fabrica.json` en
la raíz (correcto: la consola no se autogestiona como proyecto hijo).

## Triaje del Inbox

Sección `📥 Inbox` de `docs/backlog.md`: `(vacío)`. Sin entradas que procesar.

## Auditoría de estado real

Único hallazgo del tick: desde el cierre del tick anterior (10:15 UTC), la sesión interactiva del
usuario (o la propia `routine-madre-fabrica`) commiteó directo a `main` (`39bf9e4`)
`docs/reportes/EXPERIMENTO-ROUTINE-MADRE-FALLIDO.md` — un hallazgo estructural: `fire_trigger`
rechaza disparar cualquier trigger real de la fábrica porque todos se crearon desde la UI de
routines (`http_api`) y un agente solo puede disparar triggers que él mismo creó vía
`create_trigger`. Esto rompe el despacho instantáneo del PASO 4 (pool) y PASO 3 (dedicadas) de
`routine-madre-fabrica` v4 — el `lock` se sigue asignando bien (usa la API de GitHub), pero el
disparo acelerado no ocurre nunca; cada proyecto solo avanza en su tick normal de cron.

Este hallazgo contradecía directamente afirmaciones vigentes en `CLAUDE.md` § Decisiones
Arquitectónicas (Motor A-pool, y la sección de "REGLAS NO NEGOCIABLES" sobre el despachador con
latencia "≤1h") y no tenía fila en el Registro de trabajo del backlog. Corregido en este tick:

- `CLAUDE.md`: bullet del Motor A-pool ampliado con la actualización 2026-07-19; bullet de "Los
  usuarios de la consola NO tienen acceso a claude.ai/routines" corregido para no prometer latencia
  ≤1h del despachador; nuevo Error Conocido documentando el hallazgo completo (con la regla explícita
  de no presentar `fire_trigger` como garantía de latencia hasta que el usuario decida un mecanismo
  de reemplazo); Ancla de rollback actualizada al HEAD real (`39bf9e4`).
- `docs/backlog.md`: bullet nuevo en Estado general, fila nueva en Registro de trabajo para el
  commit `39bf9e4`, fila del tick 10:15 UTC corregida (decía "pendiente de push", ya integrada por
  `fabrica-sync` en `e4f2a49`), y nueva "Decisión estacionada [USUARIO]" preguntando qué mecanismo de
  reemplazo prefiere el usuario para el despacho instantáneo.

**No corregido por esta routine:** `docs/routine-madre-prompt.md` (el prompt de la routine madre
que todavía describe el PASO 4/PASO 3 sin la advertencia). No es territorio de
`routine-fabrica-consola` — es el prompt de OTRA routine, y su fix implica decidir un mecanismo de
reemplazo, no solo anotar un hallazgo. Queda para el usuario o la sesión que mantiene la routine
madre.

## Gate

Entorno re-verificado con `npm ci` + gate completo real, en verde:

- `npm run lint` → ✅ sin errores.
- `npm run test:run` → ✅ **182/182** (sin cambio — el trabajo de este tick es documentación pura,
  sin funciones puras nuevas).
- `npm run build` → ✅ (Next.js 16.2.10 / Turbopack, Node v22.22.2).

## P0 / P1 / P2

P0 y P1: sin cambios, todo `[x]`. P2 revisado ítem por ítem: sin ítems nuevos delegables — mismos
bloqueos por decisión de usuario que ticks anteriores (Refinado instantáneo y Playwright E2E
estacionados, Motor B no es v1, `tipo:"gem"` condicionado a un segundo tipo de proyecto, proxy de IA
Paquetes 1/2 fuera del alcance autónomo por gobernanza explícita), más el mecanismo de reemplazo de
`fire_trigger` (nuevo este tick, tampoco es tarea de código de este repo).

## TAREAS-MANUALES.md

Revisado: sin tareas nuevas. Solo la 🟡 3 (diseño visual y nombre) sigue pendiente sin bloquear
nada.

## Resultado

Solo documentación (`CLAUDE.md`, `docs/backlog.md`, este reporte). Rama
`claude/rutina-2026-07-19-1215-auditoria`, pusheada — `fabrica-sync.yml` la auto-mergea a `main` al
tocar únicamente `docs/**`/`CLAUDE.md`.
