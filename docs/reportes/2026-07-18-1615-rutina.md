# Reporte — Tick 16:15 UTC, 2026-07-18 (`routine-fabrica-consola`)

## Anti-solape (Paso 0)

`git fetch origin main`: HEAD en `15cebdb` (fabrica-sync: estado desde
`claude/factory-console-backlog-7jafgw`), commiteado a las 15:25:36 UTC — ~50 minutos antes de
este disparo (16:15:25 UTC), fuera de la ventana de 12 minutos. Working tree limpio, sin
worktrees ni ramas a medio mergear. Se procedió con normalidad.

## Memoria y auditoría de estado real

- `docs/backlog.md`, `docs/TAREAS-MANUALES.md` y `CLAUDE.md` leídos completos.
- Verificadas las 6 ramas `claude/*` históricas contra `main` con
  `git merge-base --is-ancestor`: las 6 ya están contenidas en `main` (incluida
  `claude/rutina-2026-07-18-0615-p1-batch`, el lote P1 que llevaba varios ticks pendiente de
  merge — ya fue mergeado por la sesión interactiva en `399111d`, junto con la activación del
  peldaño 4). Sin trabajo huérfano.
- **Hallazgo corregido:** la sección "Ancla de rollback" de `CLAUDE.md` seguía apuntando a
  `9c510d7` (estado previo al merge del lote P1), desactualizada desde que `main` avanzó a
  `399111d` y el peldaño 4 se activó. Corregida al HEAD real.
- Trigger `routine-fabrica-consola` (`trig_01NduNpiSB2NsJNuCPxmpQQp`) verificado contra
  `list_triggers`: `enabled:true`, `cron_expression:"15 */2 * * *"`, `last_fired_at` coincide con
  este disparo (16:15:08 UTC). Sin discrepancias.

## Entorno / Gate

`npm install` (448 paquetes) + gate completo corrido de verdad sobre el HEAD real de `main`:
lint ✅, `test:run` **143/143** ✅, build ✅ (Next.js 16.2.10 / Turbopack, Node v22.22.2). El
entorno puede correr el gate sin instalación previa adicional.

## Triaje del Inbox

`📥 Inbox` de `docs/backlog.md`: `(vacío)`. Sin entradas que procesar.

## Lote de este disparo

P0 y P1 están completos (`[x]`) y ya en `main`. Auditoría de P2 ítem por ítem:

- Refinado instantáneo del feedback: bloqueado — requiere reponer la tarea manual de
  `ANTHROPIC_API_KEY` (retirada explícitamente por el usuario). No delegable.
- Playwright E2E del flujo completo: ya estacionado como pregunta al usuario (repo de prueba real
  vs mocks). No delegable sin su respuesta.
- Motor B: marcado explícitamente "no es v1". No delegable.
- Promover `tipo?: "gem"` a campo real de `FabricaManifest`: condicionado a que se agreguen más
  tipos de proyecto además de "gem" — precondición aún no se cumple. No delegable ahora.
- **`eslint.config.*` no cubre worktrees de subagentes (`globalIgnores(".next/**")` anclado a
  raíz):** único ítem delegable sin decisión de usuario. Tomado como el lote de este disparo
  (marcado 🔄 antes de empezar).

### Ejecución

Un subagente `implementador` (worktree aislado, `model:'sonnet'`) resolvió el fix de ESLint:
amplió `globalIgnores` a `**/.next/**`/`**/out/**`/`**/build/**` y agregó `.claude/**`. Gate en su
worktree: lint ✅, test:run 143/143 ✅, build ✅.

Al integrar su rama y correr el gate yo mismo en el checkout principal con el worktree del
subagente aún montado, `npm run test:run` reportó **20 archivos / 286 tests** — el doble de lo
esperado. Investigado: `vitest` tiene EXACTAMENTE el mismo problema que motivó el fix de ESLint —
sin `exclude` propio, recolectaba también `.claude/worktrees/agent-*/src/**/*.test.ts` como si
fueran tests del checkout principal. Tras `git worktree remove` (ya integrado, sin cambios
pendientes) el conteo volvió a 143/143, confirmando la causa.

Lancé un segundo subagente `implementador` (worktree aislado) para el fix hermano en
`vitest.config.ts`: `exclude: [...configDefaults.exclude, ".claude/**"]`, verificado de forma
realista creando y eliminando un worktree de prueba dentro de `.claude/worktrees/` para confirmar
que la duplicación desaparece (143/143 con el worktree presente) y reproduciendo el bug original
sin el fix (286 tests) antes de restaurarlo. Gate en su worktree: lint ✅, test:run 143/143 ✅,
build ✅.

De paso, agregué `/.claude/worktrees/` a `.gitignore` — esos directorios nunca deben trackearse
en el repo principal (aparecían como untracked mientras los subagentes trabajaban).

Ambas ramas de subagente se integraron reautorando los commits en el checkout principal (para
preservar la firma de esta sesión) en vez de mergear con `--no-ff`, dejando historia lineal.
Worktrees de ambos subagentes eliminados con `git worktree remove` tras confirmar que ya estaban
integrados.

## Gate final (checkout principal, sobre el resultado integrado)

lint ✅ · test:run **143/143** ✅ · build ✅.

## Publicación

Rama designada: `claude/rutina-2026-07-18-1615-eslint-ignore`. Commits:
`921b772` (marca 🔄 + corrige ancla de rollback) · `bd01f2d` (.gitignore) ·
`b8e2933` (fix ESLint) · `88579dd` (fix Vitest). El diff toca código
(`eslint.config.mjs`, `vitest.config.ts`) además de docs/`.gitignore` — por el **peldaño 4**,
`fabrica-sync.yml` la mergeará a `main` automáticamente tras correr el gate completo en CI
(`npm ci && npm run lint && npm run test:run && npm run build`), sin necesitar acción del
usuario. Si el gate en CI fallara por algo no reproducido localmente, `main` queda intacto y el
run de `fabrica-sync` queda en rojo como señal.

## Cierre de campaña

Con este lote, **P0 y P1 están completos** y el único ítem P2 delegable sin decisión de usuario
quedó resuelto. Los ítems P2 restantes están todos bloqueados por decisión/precondición del
usuario (ver `docs/reportes/CAMPANA-2026-07-18-FINAL.md` para el detalle). No queda trabajo
delegable en este disparo más allá de lo ya hecho.
