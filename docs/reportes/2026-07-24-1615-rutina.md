# Reporte — routine-fabrica-consola, tick 16:15 UTC (2026-07-24)

Trigésimo quinto disparo de `routine-fabrica-consola` (`trig_01NduNpiSB2NsJNuCPxmpQQp`).

## PASO 0 — Anti-solape

`git fetch origin main`: último commit `eb1990a` (~1h56min de antigüedad en el momento del
fetch), merge de `fabrica-sync` del tick anterior (14:15 UTC). Working tree limpio, sin worktrees
ni ramas huérfanas (`git branch -r` solo devuelve `origin/main`). Tick procedió con normalidad.

## Memoria

Leídos `docs/backlog.md` completo, `docs/TAREAS-MANUALES.md` y `CLAUDE.md` (REGLAS NO
NEGOCIABLES, Regla de despliegue seguro, Errores Conocidos, Ancla de rollback).

## Triaje del Inbox

Sección `📥 Inbox` de `docs/backlog.md`: `(vacío)`. Sin triaje que hacer este tick.

## Auditoría de estado real

Un hallazgo (el mismo patrón recurrente de ticks anteriores): la fila del tick 14:15 UTC en el
Registro de trabajo decía "pendiente de push" pese a que `fabrica-sync` ya la había integrado en
`eb1990a` — corregida, junto con la ancla de rollback de `CLAUDE.md`.

`TAREAS-MANUALES.md`: sin tareas nuevas; solo la 🟡 3 (diseño visual/nombre) sigue pendiente sin
bloquear.

**Hallazgo nuevo — este tick rompe la racha de 23 ticks sin trabajo delegable:** `npm audit` sobre
el entorno reportó 3 vulnerabilidades de severidad ALTA que no habían sido detectadas/documentadas
en ningún tick anterior:

- `next@16.2.10`: 9 CVEs — bypass de middleware/proxy en App Router con Turbopack, DoS en Server
  Actions, SSRF en Server Actions y en rewrites (hostname controlado por atacante), confusión de
  caché de response bodies (2 variantes, una con secuencias UTF-8 inválidas), payload sin límite
  en Server Actions en Edge runtime, DoS en Image Optimization API vía SVGs, exposición no
  autenticada de endpoints internos de Server Functions.
- `postcss` (transitiva de next): XSS vía `</style>` sin escapar, lectura arbitraria de archivos
  vía `sourceMappingURL` en comentarios CSS.
- `sharp` (transitiva de next): vulnerabilidades heredadas de libvips (varios CVEs de 2026).

## Trabajo delegado

Subagente `implementador` en worktree (`isolation:'worktree'`, `model:'sonnet'`), prompt
autocontenido: bump de **solo patch** `next` `16.2.10` → `16.2.11` (fix disponible vía
`npm audit fix`, sin `--force`, ya que es compatible con el pin exacto del repo).

**Resultado del subagente:**
- `npm audit` después del bump: las 9 CVEs propias de Next.js desaparecen.
- `postcss` (8.4.31) y `sharp` (0.34.5) **siguen apareciendo** — son dependencias que el propio
  `next@16.2.11` pinea en SU `package.json` (postcss como directa exacta, sharp como opcional);
  no se resuelven con un bump de nuestro lado. No es "algo nuevo fuera de alcance": son las mismas
  2 de la lista original, ahora sin la envoltura de las 9 CVEs de Next.
- **Advertencia importante, probada y revertida por el subagente:** `npm audit fix --force` (la
  sugerencia por defecto del propio `npm audit`) intenta resolver postcss/sharp haciendo un
  **downgrade catastrófico de `next` a `9.3.3`** (versión de 2019) — en vez de arreglar nada,
  dispara el conteo a **92 vulnerabilidades (26 altas, 1 crítica)**. El subagente lo detectó,
  revirtió de inmediato y dejó la advertencia documentada en el backlog para no repetir ese
  comando en este repo.
- Gate completo del subagente en su worktree: lint ✅, test:run 182/182 ✅ (sin cambio), build ✅.
- Commit dejado en el worktree (sin push): `7a0bf5f` — solo `package.json` (2 líneas) +
  `package-lock.json` (80 líneas).

## Verificación del orquestador (regla: nunca declarar "hecho" sin verificar el diff real)

- `git show 7a0bf5f --stat` y diff de `package.json`: confirmado, solo el bump de versión, sin
  cambios fuera de alcance.
- Cherry-pick del commit a la rama designada `claude/rutina-2026-07-24-1615-security-patch`
  (`f59f229`).
- Gate completo corrido YO MISMO (orquestador) sobre el resultado, en el checkout principal:

```
npm ci
npm run lint       → ✅ sin errores
npm run test:run   → ✅ 182/182 (sin cambio)
npm run build      → ✅ (Next.js 16.2.11/Turbopack, Node v22.22.2)
npm audit          → 3 altas (postcss, sharp — pineadas por next mismo, ver arriba)
```

- Worktree del subagente eliminado (`git worktree remove --force` + `git branch -D` de la rama
  huérfana) tras confirmar que el commit ya estaba integrado en la rama designada.

## P0/P1/P2

- P0: todo `[x]`, sin cambios.
- P1: todo `[x]`, sin cambios.
- P2: nueva fila agregada documentando el hallazgo de postcss/sharp como deuda pendiente de
  upstream (sin acción disponible de nuestro lado hasta que Next.js las actualice internamente).
  Resto sin cambios — mismos bloqueos por decisión de usuario:
  - Refinado instantáneo del feedback → estacionado (requiere `ANTHROPIC_API_KEY`).
  - Playwright E2E del flujo completo → estacionado (pregunta sobre repo de prueba real vs mocks).
  - Motor B → explícitamente no es v1.
  - Promover `tipo:"gem"` a campo real del manifest → condicionado a un segundo tipo de proyecto.
  - Mecanismo de reemplazo de `fire_trigger` para el despacho instantáneo del Motor A-pool →
    estacionado, sin decisión del usuario.

## Resultado

Fix de seguridad completo (bump de patch de Next.js, 9 CVEs altos eliminados) con gate en verde,
verificado dos veces (subagente + orquestador). Rompe la racha de 23 ticks consecutivos sin
trabajo delegable. Deuda de seguridad residual (postcss/sharp) documentada en P2 como bloqueada
por upstream, no por decisión de usuario.

## Push

Rama designada `claude/rutina-2026-07-24-1615-security-patch`. El commit `f59f229` toca
`package.json` y `package-lock.json` (código) — **NO auto-mergeable por `fabrica-sync`** (solo
mergea automáticamente ramas que tocan únicamente `docs/**`/`CLAUDE.md`/`.fabrica.json`);
documentado como "pendiente de merge por el usuario" en el Registro de trabajo. La documentación
de este tick (`docs/backlog.md`, `CLAUDE.md`, este reporte) va en el mismo commit/rama por
simplicidad — el usuario revisa el diff completo (código + docs) en un solo merge.
